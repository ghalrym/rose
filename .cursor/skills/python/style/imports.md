# Imports

- **Never** use local imports (e.g. inside functions or conditionals). All imports must be at **module top level** (global scope).
- Group and order imports per Ruff/ruff: stdlib → third-party → first-party/local; alphabetize within groups.
