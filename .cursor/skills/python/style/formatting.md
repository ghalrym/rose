# Formatting

- Follow **Ruff** formatting rules. Do not deviate (e.g. line length, quotes, trailing commas, indentation). If the project has a `ruff.toml` or `pyproject.toml` with `[tool.ruff]`, respect it.
