# Heartbeat

Long-running service that coordinates agentmanager, messagequeue, and ticketmanager. Runs as user **RoseHeartBeat** (`isAgent=false`). See the main [Rose README](../README.md#heartbeat) for setup, run, and environment.
